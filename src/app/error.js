"use client"
// error page client er jonno so use client use korte hobe 

const ErrorPage = () => {
    return (
        <div>
            <h1 className="text-[30px] text-red-500">Something went wrong...</h1>
        </div>
    );
};

export default ErrorPage;