

const article = () => {
    return (
        <div>
            {/* folder er vitore arekta folder create korte hobe nested route korar jonoo */}
            <h1>This is article page</h1>
            {/* react server component sudu server e render hobe, mane amara console korle ta sudu server er console e dekhte parbo, kintu browser er console e dekhete parbo na, eta next.js by default
             */}
        </div>
    );
};

export default article;